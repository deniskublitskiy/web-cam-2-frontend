import { loadStream, loadUserStream, removeStream } from '../actions/videos';

export default class WebRTCMultiConnection {
    static bootstrap(store) {
        //store.dispatch(loadUserStream());
    }
}